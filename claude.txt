   主页（密钥获取）重构。
   现在的密钥获取页面内容太杂了，简洁点更精致哦。
   electron启动后主页，打开的electron窗口小一点，用不到现在这么大。横向的窗口，显示WeQ为标题， 也可以显示WeQ Desktop
   为标题，因为WeQ太短了。
   这时候platform已经初始完成，native实例化，可以的话这时候直接探测QQ安装数据了（不探测运行状态）
   实例化native可能遇到报错嘛，现在的后端里面有报错类型的定义，native过期前端提示版本过旧请更新，其它的还有安装损坏和恶
   意篡改，都显示安装损坏即可（自己实现弹窗显示错误，别用原生alert）   标题下方显示两个按钮，左侧：现有的账号配置
   右侧： 新的开始  如果没有一个userconfig，左侧按钮灰色无法点击。
   先说探测QQ状态，把QQ执行文件地址，wrapper.node地址，login.db地址等等有用的，在service.bootstrap里面新开一个服务，glo
   balConfigService 全局配置服务，探测完成后记录进去，之后优先读取配置，验证目标路径是否存在，不存在重新探测更新配置。
   再说右侧的按钮，新的开始，左半边大概占30%
   的宽度，作为登录选项，中上方，一个巨大的头像，下面粗体昵称，在下面正常粗细QQ号，如果没有获取到昵称（login.db解密失败
   的fallback）就粗体QQ号。如果有多个账号，这两个的右边显示向下的箭头号，点击下拉账号选择框（每个账号是左侧头像，右侧上
   一行昵称，下一行QQ号，账号之间画线分割，鼠标悬浮动画，点击切换）
   在下面是数据库密钥的预留框，允许用户手动输入，框的右侧是最终的按钮，当用户输入内容是16位ascii时，从获取密钥，变成点
   击进入。   用户没输入，框内显示等待获取/填入账号对应的数据库密钥。 用户输入后提示填入16位密钥
   除了用户输入的最后一位，前面的字符全部*显示，给一个眼睛图标（记得不要用这种：👀，用React等等组件图标库！！） 查看。
   右侧70%宽度的区域显示探测数据    左上方一个较大的QQlogo 右侧时它对应的探测信息，简单三个内容，1：version
   版本号（wrapper.node的路径里面有出现"QQ.exe所在的目录\versions\9.9.28-46928\resources\app\wrapper.node"）
   用户数据路径： Tencent Files那个            下方是几个大数字显示： x 个用户数据 （Tencent
   Files目录下面的数字子目录）  x 个 历史登录账号   x 个在线实例。
   关于未预期异常的处理：  如果没有QQ.exe 直接弹窗报错安装， 如果没有wrapper.node弹窗QQ损坏或版本不支持。
   如果没有用户数据目录，在合适的位置显示选择按钮（是选择不是输入，拉起目录选择器）
   如果没有login.db直接静默回退fallback获取机制即可。                    关于在线账号探测的时机和探测的方法：
   轮询调用native函数获取QQ在线的主pid，因为我们只要个数嘛，现在不探测每个pid对应什么
   然后用户点击获取密钥的时候我们探测一次，探测结果直接用于分发获取路径。  唯一需要改进的地方是：
   当只有一个QQ主进程时，改变我们的探测方式：
   遍历所有的已知QQ号，做这个互斥锁探测，相关方法在nt_helper.node中已实现，直接用就好。  const { isQqLoggedIn } =
   require('../index.js');
   const UINS = ['1707889225', '1707889226'];
   for (const uin of UINS) {
       const ok = isQqLoggedIn(uin);
       console.log(`${uin}: ${ok ? '已登录' : '未登录'} (${ok})`);
   }
   这个失败了fallback之前的那套探测逻辑。
   记得账号选择器限制高度，滚动选择，最下方给一个登录新的账号的按钮，登录新的账号不需要输入QQ号，输入了也是白输入，nati
   ve用不上。            依赖在线QQ客户端获取失败直接报错，让退出登录后重试。     快速登录失败fallback二维码登录，
   二维码登录失败弹窗说明错误。   显示二维码直接在原来头像位置显示即可。
   还没说完，右侧上方不是QQ信息探测嘛，下方可以搞一点统计图表，参考github的项目的语言比例，一根横线，彩色的分割，找账号
   目录下前八大小的数据库文件，横线下方图例，原点颜色  xxx.db  xxxxxmb/gb  大概就这样
   宽度不够右侧显示一个文件空间占用的统计图，用高级一点的图标，重点是主题色统一，不要五颜六色了。 统计几个Tencent
   Files\QQ号\nt_qq\nt_data 目录下，子目录的大小（小文件特别多的时候这个统计会不会很耗时，耗时的话记得占位动画）
   左侧切换账号时，右侧的与统计要跟着改变，要有显示动画，例如横线从左到右拉开。      重点重点：
   主题色要统一，现有的字体和背景可以复用，至于间距和排版，你按实际情况定，要留白但是间距不能太大。
   左侧按钮现有的配置点击界面和右侧一样，只是省掉密钥获取按钮，密钥框和依旧存在，眼睛变成复制，点击复制，右侧统计依旧，
   所以QQ的安装探测不能省略（当然前面说了，保存到全局配置，这里检查一下即可）
   关于账号选择，看userconfig里面有没有存入昵称，头像，QQ号三要素，账号选择器等逻辑不变，去掉添加新账号的按钮。
   值得注意的是，现有账号配置不应影响新的开始中的获取，唯一主键是用户数据库目录，就是这个目录D:\estkim\T\Tencent
   Files\1707889225  如果没有userconfig里面需要添加（因为没有的话，未来解密备份数据库会冲突）
   只要数据库路径一样，只是更改用户配置而已
   如果账号一样但数据库目录不一样，userconfig里面要当一个全新的用户来记录，怎么命名看你了。
   前端的美观性是最重要的，加适当的动画，现有风格可以复用，改的是结构
   我说的非常的多，感觉你记不住，我在本地写个文档吧，忘了就参考它实现。    加油喵，期待你的前端重构！！