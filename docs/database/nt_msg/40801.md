# 40801：消息装扮信息

`40801` 是 `c2c_msg_table`、`group_msg_table` 和 `dataline_msg_table` 中的消息装扮列，保存每条消息对应的气泡、聊天字体和挂件等信息。

截至 **2026 年 8 月 23 日**，WeQ 已完成该列的主要字段解析。该列本质上是一个 protobuf：外层使用 tag `40801` 包裹，真正的装扮字段位于内层消息中。

对应实现：

| 文件 | 职责 |
| ---- | ---- |
| `packages/codec/src/proto/msg/40801.ts` | `40801` 的 wire schema |
| `packages/codec/src/domain/msg/msg_dress.ts` | 解码并提取气泡、字体、挂件 ID |
| `packages/service/src/account/msg_decoration.ts` | 根据 ID 解析装扮资源 |

---

## 一、整体结构

```text
40801 (BLOB)
└── tag 40801 (LEN / nested)
    ├── tag 41510  bubbleId
    ├── tag 41525  fontId（优先）
    ├── tag 41528  widgetId
    └── tag 41531  fallback font value（备用字体 ID）
```

外层 `40801` 是列本身的 protobuf 字段，内层字段不能直接按列号解释。

## 二、已确认的装扮字段

| 内层 tag | 字段名 | 类型 | 含义 | 备注 |
| ---: | ---- | ---- | ---- | ---- |
| `41510` | `bubbleId` | `uint32` | 气泡皮肤 itemId | 直接使用 |
| `41525` | `fontId` | `uint32` | 聊天字体皮肤 itemId | 字体 ID 的首选来源 |
| `41528` | `widgetId` | `uint32` | 挂件 itemId | 直接使用 |
| `41531` | `flag41531` | `uint32` | 备用字体值 | 需要按规则解码后才能得到字体 ID |

## 三、字体 ID 获取规则

字体 ID 的读取顺序如下：

1. 如果 `41525` 存在且不为 `0`，直接将 `41525` 作为字体 ID。
2. 如果 `41525` 为 `null` 或 `0`，读取 `41531`，记为 `stored`。
3. 使用下面的公式解码：

```text
fontId = ((stored & 0xFF) << 8) | ((stored >> 8) & 0xFF)
```

4. 解码结果为 `0` 或没有结果时，视为未设置聊天字体。

例如：

```text
stored = 0x1234
fontId = 0x3412
```

这里的 `41531` 不是直接可用的字体 itemId，而是一个低 16 位字节序交换后的值。

## 四、其他已观测字段

以下字段已经纳入 schema，可保证解码和保真，但语义暂不作为装扮 ID 使用：

| tag | 类型 | 当前说明 |
| ---: | ---- | ---- |
| `41501` | `uint64` | 未知签名 / uid hash |
| `41502` | `uint32` | proto 版本或标志 |
| `41511` | `uint32` | 未知 |
| `41512` | `uint32` | 气泡版本 / revision |
| `41513` | `bool` | 未知标志 |
| `41514` | `uint32` | 未知小整数 |
| `41516` | `uint32` | 未知小整数 |
| `41517` | `bool` | 未知标志 |
| `41518` | `uint32` | 少量出现，可能是其他 itemId |
| `41519` | `uint32` | 字体相关整数 |
| `41520` | `uint32` | 字体标志 |
| `41521` | `uint32` | 字体子标志 |
| `41522` | `uint32` | 字体子标志 |
| `41523` | `bool` | 未知标志 |
| `41524` | `uint32` | 与 `41519` 相似的字体相关值 |
| `41529` | `bool` | 未知标志 |
| `41530` | `uint32` | 未知 itemId |
| `41532` | `bool` | 未知标志 |
| `41533` | `bool` | 未知标志 |
| `41534` | `bool` | 未知标志 |
| `41535` | `uint32` | 未知整数 |

此外还观测到少量更高 tag，例如 `41538`、`41665`、`42350`、`43301`、`43302`、`52180`、`52182`、`52346`、`52381`、`52382`。这些字段目前只做 wire 层兼容，不参与气泡、字体和挂件 ID 提取。

## 五、解码后的使用链路

`decodeMsgDressColumn` 输出统一结构：

```ts
{
  bubbleId: number;
  fontId: number;
  widgetId: number;
}
```

后续资源解析和渲染只使用这个统一结构：

- `bubbleId` 用于解析气泡资源；
- `fontId` 用于获取和注入聊天字体资源；
- `widgetId` 用于解析挂件资源。

当三个 ID 都为 `0` 时，解码结果为 `null`，表示该消息没有可用的装扮信息。

---

[返回 nt_msg 数据库文档](./index.md)
